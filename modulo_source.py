def sumar(a = 0, b = 0 ) -> int:
    print('resultado para sumar(a, b) desde %s ubicado en %s' % (__name__, __file__))
    return a + b

def restar(a:int = 0, b:int = 0 ) -> int:
    print('resultado para restart(a, b) desde %s ubicado en %s' % (__name__, __file__))
    return a - b

# Ejecuta modulo.py
